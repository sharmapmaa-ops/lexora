pdfplumber>=0.11
pypdfium2>=4.0
pypdf>=5.0
pytesseract>=0.3.10
Pillow>=10.0
python-docx>=1.1
reportlab>=4.0
arabic-reshaper>=3.0
python-bidi>=0.4
opencv-python-headless>=4.8
numpy>=1.24
psycopg[binary]>=3.1
# Aspose Cloud test pipeline (py/aspose_test_pipeline.py) - isolated
# experiment, not used by the real translation pipeline.
aspose-words-cloud>=25.0
asposepdfcloud>=25.0
pycryptodome>=3.20
# Source-layout-driven reconstruction (py/source_reconstruction.py) -
# real embedded image byte extraction from the source PDF, including
# soft-mask/transparency compositing (pdfplumber alone doesn't expose
# usable raw image bytes for this).
pymupdf>=1.24
# OCR service router (py/ocr_router.py) - reuses pdfplumber (already
# above) and aspose-words-cloud (already above); no new dependency of
# its own.
