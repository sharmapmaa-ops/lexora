{
  "name": "Lexora AI Solutions",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",

  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "Lexora AI Solutions",
      "onAutoForward": "openPreview"
    }
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tesseract-ocr tesseract-ocr-ara tesseract-ocr-hin tesseract-ocr-fra tesseract-ocr-spa tesseract-ocr-deu && (pip install -r requirements.txt --break-system-packages || pip install -r requirements.txt)",
  "postStartCommand": "bash .devcontainer/start-server.sh",
  "postAttachCommand": "bash .devcontainer/start-server.sh"
}
