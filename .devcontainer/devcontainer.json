{
  "name": "Lexora AI Solutions",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",

  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "Lexora AI Solutions",
      "onAutoForward": "openPreview"
    }
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tesseract-ocr && (pip install -r requirements.txt --break-system-packages || pip install -r requirements.txt)",
  "postStartCommand": "bash .devcontainer/start-server.sh"
}
