services:
  - type: web
    name: lexora
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 py/server.py
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
