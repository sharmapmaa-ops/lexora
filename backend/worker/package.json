{
  "name": "lexora-translation-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side background worker for the Translation service's pdf.js text-layer pipeline (js/engine-translation.js: buildPdfjsTranslatedDocxBlob), ported to Node so a translation job keeps running on the server after the browser navigates away or closes - see JOB_SYSTEM.md.",
  "dependencies": {
    "pdfjs-dist": "3.11.174",
    "pdf-lib": "1.17.1",
    "@pdf-lib/fontkit": "1.1.1",
    "jszip": "3.10.2",
    "canvas": "2.11.2"
  }
}
